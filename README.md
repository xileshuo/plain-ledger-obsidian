# PlainLedger
Obsidian 独立记账面板（LifeOS）。账单、分类、订阅规则保存在库内 JSON，随库同步。
当前公开版本：**3.2.4 · 公版（需激活）**
## 安装（BRAT）

1. 安装并启用社区插件 **BRAT**
2. 添加 Beta 插件仓库：

```text
https://github.com/xileshuo/plain-ledger-obsidian
```

3. 启用插件；之后可在 BRAT 检查更新

## 激活

当前公开分发为 **公版**：需激活码后使用全部功能。新设备请复制侧栏/设置中的设备指纹，联系作者获取激活码。已激活设备升级后一般无需重新激活。
手动安装：下载 [Latest Release](https://github.com/xileshuo/plain-ledger-obsidian/releases/latest) 中的插件文件到 `.obsidian/plugins/plain-ledger/`。
## 界面展示

### 总览

<img width="1900" height="1200" alt="记账" src="https://github.com/user-attachments/assets/0a1a1625-d632-4c44-8bfd-cbd1cb5bc558" />


### 手机端
<img width="435" height="869" alt="iShot_2026-08-08_09 04 48" src="https://github.com/user-attachments/assets/0aa39850-607c-470c-8630-5609ea9781ec" />
<img width="438" height="877" alt="iShot_2026-08-08_09 05 20" src="https://github.com/user-attachments/assets/a1945007-59d5-4181-99d5-d123d2f992ed" />

## 使用说明
> **PlainLedger** 是运行在 Obsidian 里的独立记账面板——账单、分类、订阅规则都保存在**你的库内 JSON**，随 Obsidian / iCloud / Git 同步，不依赖第三方账 App，也不把数据上传到云端服务。

**和记帐 App 的区别：**

\`\`\`text
记帐 App：数据在 App 里，和笔记割裂
PlainLedger：数据在库里，和日记 / 项目 / 复盘同屏共存
\`\`\`

**核心优势一览：**

| 能力 | 说明 |
|------|------|
| 库内自持 | \`Finance/PlainLedger/ledger.json\` 纯文本，可备份、可 diff、可脚本处理 |
| 三种录入 | 智能一句话、手动表单、截图 OCR（支付宝 / 微信账单截图） |
| 订阅 & 周期 | 续费提醒、待入账、分期 / 周期规则，到期一键入账 |
| 月结结转 | 每月 1 日自动将当年累计净收支结转至当月（可关） |
| 多维视图 | 底栏 **账本 · 统计 · 记账 · 报表 · 设置**，口径统一；日历在账本内切换 |
| 日记联动 | 记账后可选追加一行到当日 \`.md\` 笔记 |
| 全平台 | Mac / Windows / iOS / Android Obsidian 同库同步 |

---

## 目录

- 一、PlainLedger 适合谁
- 二、安装与文件说明
- 三、如何打开面板
- 五、记账：三种方式
- 六、账本：一眼看懂本月
- 七、账本 · 统计 · 报表
- 八、订阅 · 待入账 · 周期
- 九、设置页说明
- 十、数据文件与备份
- 十一、更新说明
- 十二、常见问题

---

## 一、PlainLedger 适合谁

- 已经在用 Obsidian 管理生活 / 工作，希望**记账和笔记在同一体系**
- 不想 monthly 再开一个 App，又希望有**预算、趋势、报表、日历**
- 需要**订阅 / 续费 / 分期**提醒，而不是只记流水
- 重视**数据所有权**——JSON 在本地，导出 Excel / CSV 随时可做

---

## 二、安装与文件说明

**最小安装（推荐）：**

\`\`\`text
你的库/.obsidian/plugins/plain-ledger/
├── main.js
├── manifest.json
├── styles.css
└── default-ledger.json   ← 公版样例 / 可选
\`\`\`

**可选：** \`vendor/lang/\` 仅在使用**截图 OCR** 且希望离线识别时需要；不用截图可省略。

**自动生成（无需打包）：**

| 文件 | 说明 |
|------|------|
| \`.obsidian/plugins/plain-ledger/data.json\` | 插件设置（预算、激活码等），Obsidian 首次保存时自动创建 |
| \`Finance/PlainLedger/ledger.json\` | 真实账单数据，首次记账或导入后生成 |

安装后在 **设置 → 第三方插件** 启用 PlainLedger，建议重载一次。

> **版本包命名**：界面与设置页显示的「公版」指需激活的公版；下载文件夹名可能写作「公版（商）」——二者指同一版本。「个人版」同理。

---

## 三、如何打开面板

- 左侧 Ribbon **钱包图标**
- 命令面板：**打开 PlainLedger 面板** / **记账**
- 底部 Tab：**账本 · 统计 · 记账 · 报表 · 设置**（记账为底栏居中按钮）

---

个人版无需激活，首次打开即可使用。

### 公版（需激活码）

1. 安装并启用插件后，**首次打开会自动加载**完整分类（18 类 · 117 二级）与 **6 笔示意账单**
2. 在侧边栏激活页复制设备指纹 \`PLG-XXXXXXXX\`（绑定 **Obsidian 库名称**）
3. 发给作者获取 \`KEY-XXXXXXXX\`，输入激活码并点击 **激活**
4. 激活成功后 **永久有效**（同一库 Mac / iPhone 共享）

2. 点击后 **自动注入** 完整分类（18 类 · 117 二级）与 **6 笔示意账单**，无需手动恢复样例
3. 试用期间全功能可用；到期后须输入激活码才能继续记账，**库内数据不会丢失**
4. 试用中可随时输入激活码转为永久版

> **更新日志**与**完整使用说明**不会在启动时自动弹出；请在激活页或 **设置 → 数据 / 关于** 手动打开。

---

## 五、记账：三种方式

### 智能录入（推荐）

输入自然语言，例如：

\`\`\`text
午餐 28 餐饮
打车 35 交通
工资 15000 工作收入
\`\`\`

支持金额、分类关键词、时间词（昨天 / 上周三等）。可在 **设置 → 全局关键词** 定制映射。

### 手动录入

选一级 / 二级分类、金额、时间、备注、是否报销等，适合精确补账。

### 截图 OCR

粘贴支付宝 / 微信账单截图，自动识别金额与商户（首次需准备 OCR 语言包，可离线 \`vendor/lang\` 或联网下载）。

---

## 六、账本：一眼看懂本月

- **本月支出**：当前自然月支出合计，右侧显示收入与月度结余；支出旁显示较上月环比
- **月度结余**：当月真实收支（**不含**月结结转行）
- **年累计结余**：当年 1 月起累计（**不含**结转）
- **预算环**：设置本月预算后显示进度，超支高亮
- **待入账**：订阅 / 周期到期项，一点入账
- **账单列表**：按日分组，支持搜索、分类筛选、报销筛选

**月结行：** 每月 1 日系统自动生成，只读；日合计「支」不含月结。可在 **设置 → 基础与预算 → 月结自动结转** 关闭。

---

## 七、账本 · 统计 · 报表

| Tab | 你能看到什么 |
|-----|----------------|
| **账本** | 本月横幅、预算、待入账、账单列表；可切换 **日历** 热力视图 |
| **统计** | 汇总表（10 行滚动 + 表底总计同/环）· 时间行 ⓘ 提示 · 消费洞察 |
| **记账** | 底栏居中按钮，智能 / 手动 / 截图记账 |
| **报表** | 分类占比环图（含「其他」桶）、主题筛选、分类明细；筛选状态会记住；手机端搜索框与收入/支出、一级/二级下拉同排显示 |

各页「结余 / 支出」口径与账本一致，避免不同 Tab 数字对不上。

---

## 八、订阅 · 待入账 · 周期

在 **设置 → 规则** Tab（与常用、分类、数据并列）：

- **订阅服务**：Netflix、iCloud+ 等续费周期，到期进入待入账
- **周期 / 分期**：固定周期扣款或分期计划
- **待入账**：首页与设置均可一键确认入账

适合管理「还没扣款但已知将要发生」的支出，避免月底才发现漏记。

---

## 九、设置页说明

Obsidian **设置 → 第三方插件 → PlainLedger** 与面板激活页 **高级配置** 为同一入口。桌面端标题格式为 **PlainLedger 配置 · 个人版 v3.2.4**（版本号在标题末尾）。

| Tab | 内容 |
|------|------|
| **常用** | 基础与预算、日记联动 |
| **分类** | 一级 / 二级分类管理 |
| **规则** | 待入账、订阅、周期、全局关键词 |
| **数据** | 导入导出、数据目录、快速上手 / 使用说明 / 更新日志 |
| **关于** | 作者、版权、其他作品；亦可打开使用说明 |

---

## 十、数据文件与备份

| 路径 | 内容 |
|------|------|
| \`Finance/PlainLedger/ledger.json\` | 账单、分类、订阅、周期规则 |
| \`.obsidian/plugins/plain-ledger/data.json\` | 插件设置 |

**建议：** 定期 **设置 → 数据管理 → 导出 JSON**；大改前先导出一版。

导入支持 **PlainLedger JSON**（整包替换）与 **Excel**（默认合并流水，保留订阅 / 周期规则）。

---

## 十一、更新日志

升级后**不会**在启动时自动弹出更新日志。

随时可在 **设置 → 数据**（或 **关于**）或激活页手动打开（本次更新默认展开，历史版本折叠）。

---

## 十二、常见问题

不需要。点击 **「开启试用」** 后会自动写入分类与 6 笔示意账单。

### Q：公版样例数据能删吗？

可以。激活后清空或导入自己的 Excel / JSON 即可。

### Q：为什么没有 data.json？

\`data.json\` 是 Obsidian 在保存设置时**自动生成**的，安装包里没有是正常的。

### Q：vendor 文件夹必须吗？

不必须。只有截图 OCR 且希望离线识别时才需要 \`vendor/lang/\`。

### Q：升级后还会自动弹使用说明或更新日志吗？

不会。启动时不再自动弹出任何引导或更新弹窗；**快速上手**、**使用说明**、**更新日志**均在激活页或 **设置 → 数据 / 关于** 手动打开。

---

## 一句话总结

**Obsidian 是你的知识库，PlainLedger 是你的账本——数据留在库里，记账、看统计、管订阅，都在同一个面板完成。**
## 更新日志
### 3.2.4

- 暗色：危险按钮/日历提示/订阅选中芯片/常用分类与更新弹窗行边框对齐 LifeOS token

### 3.2.3

- 账本：首页年/月/周三卡保留图标与数额，去掉「本年本月本周」文字（移动与桌面同步）
- 设置：Tab 选中态统一 accent 36% 填充（与总年月周一致）
- LifeOS：主 CTA / 更新按钮字色改用 --lifeos-on-accent（暗色可读）
- 暗色：芯片/卡片/分类格等白半透明改走 --plg-chip-bg / --plg-surface-soft（含记一笔弹层）
- 控件：--plg-seg-h 桌面 28 / 手机 32，报表下拉对齐；顶栏 Tab 高与分段一致
- 间距：LifeOS 阶梯 --plg-space-1/2/3=4/8/12，xs/sm/md 与 --lifeos-space-* 别名对齐
- 弹层：关闭钮 32×32 透明底、标题 16/800，对齐 lifeos-ui-shared
- 强调色：列表筛选/金额聚焦等改用 --lifeos-accent；桌面底 inset 不再套手机 72px
- 无障碍：prefers-reduced-motion 覆盖 Tab/FAB/滑块/设置聚焦脉冲；报表筛选条去双框改分割线

### 3.2.2

- 报表筛选区：输入框 / 支出·一级 / 分类芯片与外框拉开边距，避免压线
- 暗色：本月待入账条与本月预算数字框配色修正

### 3.2.1

- 分类：编辑一级时删除有账单的二级，改为必选迁移目标，禁止静默删除
- 报表：搜索输入防截断（IME 友好 + 只刷新结果区，键盘不收）

### 3.2.0

- LifeOS：主 CTA / 分段选中统一 --lifeos-accent；记账去 glass 实底；控件阶梯 --plg-seg-h / --plg-action-h
- 记账：无分类空态走 lifeos-empty-state；模式 Tab 按压反馈；收键盘允许壳高回缩
- 记账：文案统一「记账」；手动表单行距恢复；再记/保存区去底色；支出↔收入锁壳高不闪缩
- 记账：常用分类暖色底；插件桌面两行四列（Mac 八列单行）；分类区固定两行高
- 纪念日日历：无框线沉浸对齐记账；跨月低对比；手机不透明底防重影
- 手机记账：再记/保存/粘贴/识别对齐输入框宽高；金额去¥、备注居中
- 手机记账：手动底栏贴底对齐智能粘贴/识别；识别按钮补图标；表单行距加大
- 日历：补齐跨月日期（低对比）、法定放假/调休班、周末节假着色
- 手机记账：再记/保存与智能粘贴/识别同套配色（保存为主色 CTA）；手动底栏边距对齐
- 手机记账：壳高按键盘弹起后锚定，避免弹出后再二次收缩
- 手机记账：再记/保存/粘贴/识别与模式 Tab 同高细长条；表单行距加大；同步徽章去底色沉浸
- 手机记账：再记/保存压矮，表单行距加大；再记改为暖色浅底
- 记一笔手动：分类区去底色沉浸；一级分类改为无痕上下滚动；提示文案与支出/报销行垂直居中
- 记一笔：弹层自带选中底色 token（修复桌面/移动无选中态）；标题分割线强制去除
- 手机日历：月汇总收入/支出/超支（或结余）单行排布
- 手机账单：日标题补充收入与支出；搜索聚焦时收起顶部固定区，避免键盘遮挡
- 手机账单：搜索态收起英雄区但保留安全顶距与底栏，避免叠状态栏、底栏消失
- 手机记一笔：粘贴/识别略加长并居中；去掉按钮上方分割线
- 手机记一笔：三 Tab 锁定首次壳高锚点，切手动/截图不再拉高；智能示例精简为一行
- 空白账本：可选只种子分类（无示意流水），便于从零记账
- 导入：取消文件选择后回到欢迎页，不再卡在空白中间态
- 设置：Tab 按钮改为 action 样式，层级与 BrainCore / 纪念日一致
- 账本：首页横幅标签固定为「本月 · 支出」，与统计周期选择解耦
- 记一笔：无分类时空状态 CTA「去添加分类」直达设置分类页
- LifeOS：作者主页 authorUrl 三插件统一为同一链接
- 微交互：底栏 Tab 与总年月周胶囊补齐按压缩放（尊重 prefers-reduced-motion）
- 手机端：报表/统计切换「总年月周」时同步选中态与周期导航（修复数据已变、标签仍停在原项）
- 电脑端：报表/统计「总年月周」与筛选选中态同步刷新，并给强调色底色反馈
- 暗色模式：账本/统计/报表玻璃顶栏与摘要卡改为深色表面；总年月周选中字色/底色对比可读；环心/筛选/标签同步适配
- 暗色模式：同步徽章对比度；账单列表图标槽底色；强调色按钮字色 token
- 手机端：底栏账本/统计/报表与账本内账单/日历切换即时着色，选中态更易辨认
- 记一笔：暗色模式输入区/示例文案/Tab/粘贴按钮对比度修复，避免白半透明磨砂导致看不清
- 记一笔：预览「修改」不再关掉记一笔面板；删除确认「迁移」改为「改分类」
- 记一笔：关闭面板时拆除日期点外关闭监听，避免泄漏
- 记一笔：桌面分类固定列宽、两行横向滚动；主按钮/Tab/支出收入强调色统一 --lifeos-accent
- 记一笔：截图区文案提示可「填入智能」；popover 尊重 prefers-reduced-motion
- 记一笔：移动端无论键盘是否弹出，底部始终预留同一键盘区；支出/收入切换条压矮
- 记一笔：移动端「已选」跟支出/收入同行；少分类单行；日期点外关闭并修空白
- 记一笔：移动端去掉「常用」；支出/收入不再拉满整行；未选时隐藏重复提示；恢复「截图」Tab
- 记一笔：金额限两位小数；清除有 Notice；智能清除同步清空「本次记录」
- 记一笔：OCR 成功预览与低置信均可「填入智能」；二级 popover 按内容测高
- 记一笔：模式 Tab 轻交叉淡入；尊重 prefers-reduced-motion
- 记一笔：已选分类实时提示「已选：…」；文案改为「点开选择」；金额硬校验；入库失败可见反馈
- 记一笔：预览 CTA 统一为「修改 / 再记 / 确认入账」；OCR 失败可一键「填入智能」；Esc 关闭
- 记一笔：分类+常用同一底色区（无框线），表单沉浸无框，Tab / 保存行底色分层；轻动效
- 记一笔：移动端底部预留约 40% 键盘区，界面始终在键盘上方（vv 报高时再抬高）；分类键盘可达
- 记一笔：模式提示行右侧补回「清除」，可清空当前页草稿（智能/手动/截图）
- 记一笔：手动一级分类改为每行 8 个；面板宽高随网格收缩，去掉分类区底部多余空白
- 记一笔：金额/备注/日期/时间标签内联到输入框前；日期与时间统一系统滚动选择
- 记一笔：手动页布局对齐 BrainCore For Mac；金额框 ¥ 前缀；主按钮「保存」
- 智能识别：增强今天/昨天/前天/大前天/今日/明天，以及 6.5 / 6月5日 / 2026-6-5 等日期写法
- 智能识别：强化工资/薪水/奖金/收款/进账等收入词，以及支出/花了/消费等支出词
- LifeOS：三插件视觉/交互全面对齐——强调色统一改用 --lifeos-accent（Tab 选中、状态行、关于页链接），不再随主题 accent 漂移
- 反馈：Notice 提示音统一去除表情符号前缀，三插件文案口吻一致
- 微交互：设置页与侧边栏按钮补齐按压态反馈（轻微缩放），告别冷热不均
- 试用横幅：点击行为统一跳转到「授权」设置页
- BrainCore：设置页在未激活时锁定「路径/模块/习惯」，仅保留「授权」「数据」「关于」，与 PlainLedger / 纪念日一致
- BrainCore：空状态视觉去除斜体低透明度的「将就感」，与其余两插件视觉权重对齐
- 入口：Ribbon 图标文案统一为「打开 + 产品名」（纪念日 / BrainCore）

### 3.1.0

- LifeOS：manifest 与 PLUGIN_VERSION 统一为 v3.1.0；出包日期随构建时间更新
- LifeOS：侧栏 inset 规则抽取为共享 CSS，三插件构建时注入
- 设置：Tab 层级与缩进对齐 BrainCore / 纪念日；公版未激活时顶部锁定提示
- PlainLedger：移动端侧栏左右留白统一 10px（原 16px）
- 清理：移除未使用的 LifeOS 插件族页脚；试用按钮文案统一为动态小时数
- 文档：使用说明补充「界面徽章公版 ≠ 文件夹公版（商）」命名说明

### 3.0.6

- 修复：设置 → About → 更新日志「打开」无反应（BrainCore 缺少 addClasses；关闭设置后等待再弹窗）

### 3.0.5

- LifeOS：About「打开设置」跳转修复；About 桌面三列卡片布局
- 记一笔：移除旧 plg-capture-modal-mobile CSS，统一 overlay 路径
- BrainCore：使用说明改为库内 .md；修复加载失败
---
## 闭源说明
本仓库仅用于 **BRAT / GitHub Release 分发** 与产品介绍，不包含插件源码。
仓库：https://github.com/xileshuo/plain-ledger-obsidian
